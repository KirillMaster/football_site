# fail2ban filter — FC Arsenal-92
# Ловит флуд, которому nginx отдал 429 (limit_req: превышение rate).
[Definition]
failregex = ^<HOST> - \S+ \[[^\]]+\] "[A-Z]+[^"]*" 429
ignoreregex =
datepattern = %%d/%%b/%%Y:%%H:%%M:%%S
